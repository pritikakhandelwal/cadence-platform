# Copy to .env.local (git-ignored) for local development.
# Mirrors the DATABASE_URL / REDIS_URL convention in the root .env.example:
# read via process.env with an inline fallback, no dotenv loader assumed.

NEXT_PUBLIC_API_URL=http://localhost:8000

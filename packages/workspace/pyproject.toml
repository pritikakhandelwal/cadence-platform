[project]
name = "cadence-workspace"
version = "0.1.0"
description = "Shared per-analysis file workspace helpers, used by apps/api and apps/worker."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cadence_workspace"]
